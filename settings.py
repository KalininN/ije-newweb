import os

IJE_PATH = "."


CONFIG_PATHS = {
    "contests": os.path.join(IJE_PATH, "acm.xml"),
    "results_dir": os.path.join(IJE_PATH, "results"),
    "archive_dir": os.path.join(IJE_PATH, "archive"),
    "solutions_dir": os.path.join(IJE_PATH, "sols_acm_reg"),
    "ije": os.path.join(IJE_PATH, "ije_cfg.xml")
}
